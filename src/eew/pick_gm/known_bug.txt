./pick_gm ./pick_gm.d ./2016304.tank

알려진 버그
1. earthworm 7.9 (64bit) 에서 pick_gm이 제대로 동작하지 않았다. (오프라인동작시에만 해당)
   WaveMsg2MakeLocal() 함수에서 -1을 리턴하지만 소스추적(swap.c) 결과 원래함수에서는 0을 리턴하고 있었다.
   무엇이 원인인지는 모르겠지만, 7.6으로 컴파일했을때는 제대로 동작했다.
   이것이 단순히 이 서버(99)만 그런지는 잘 모르겠다.

-> 해결된. 읽는 부분에서 long과 int문제였음..